# Wrapper-only dependencies. vLLM is installed separately in the Dockerfile
# from its own published wheel so the version pin lives there.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
httpx>=0.26.0
