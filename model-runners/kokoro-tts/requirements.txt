# Kokoro TTS — Apache 2.0 (pip name may be `kokoro` or `kokoro-tts` depending
# on the upstream release; the runner tries both at import time)
kokoro>=0.7.0

# Audio handling
numpy>=1.24
soundfile>=0.12

# Safebox FastAPI service stack
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
